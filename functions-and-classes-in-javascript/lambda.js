// Sample Lambda Function in JavaScript

const addNumbers = (a, b) => {
    return a + b;
};

// Example usage
console.log(addNumbers(5, 3)); // Output: 8

// Example of a lambda function with no parameters
const greet = () => {
    console.log("Hello, World!");
};

greet();